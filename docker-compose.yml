services:
  sih-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sih-app
    restart: always
    ports:
      - "80:3000"
    env_file:
      - .env.local
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOSTNAME=0.0.0.0
